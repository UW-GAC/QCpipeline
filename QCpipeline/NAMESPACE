import(GWASTools)
import(gplots)
import(grid)
import(gridBase)
import(hexbin)
import(Biostrings)
import(quantsmooth)

export(readConfig, boxplotMeanSD, plot2DwithHist,
       bestLegendPos,
       dbgapScanAnnotation, dbgapSnpAnnotation,
       make.allele.mappings, make.allele.annotation,
       chromIntensityPlotIdeogram, anomStatsPlotIdeogram)
