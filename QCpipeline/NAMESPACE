import(GWASTools)
import(IRanges)
import(S4Vectors)
import(GenomicRanges)
importFrom(gplots, plotCI) # used in boxplotMeanSD
import(BiocGenerics)
import(Biobase)
import(tools)
import(GWASExactHW)
import(gdsfmt)
importFrom(stringr, str_sub, str_locate_all, str_replace_all, "str_sub<-") # used in laiDosageFile
import(grid) # used in multiplot
importFrom(R.utils, countLines) # used in filterImputationSegments

export(readConfig, writeConfig, setConfigDefaults,
       boxplotMeanSD, plot2DwithHist, bestLegendPos,
       dbgapScanAnnotation, dbgapSnpAnnotation,
       defineFamilies,
       GenotypeReader, IntensityReader,
       plateLayoutPlot,
       findRelSet,
       hweSimulate,
       hweSimulateGenotypeMatrix,
       quadSolveMAF,
       qqPlotPng, manhattanPlotPng,
       gdsCombine, checkGdsCombine,
       gdsMerge, gdsMergeCheck,
       chromToChar, chromToInt,
       filterImputationSegments,
       getAnomSegOverlap,
       calculateLambda,
       laiDosageFile, checkLaiDosageFile,
       multiplot,
       expRelsCategory,
       ADFtoScanADF
       )
