import(GWASTools)
import(IRanges)
import(GenomicRanges)
importFrom(gplots, plotCI) # used in boxplotMeanSD
import(BiocGenerics)
import(Biostrings)
import(tools)
import(GWASExactHW)
import(gdsfmt)
#importFrom(SRAdb, IGVclear, IGVgenome, IGVload, IGVgoto, IGVsort, IGVsnapshot, IGVcollapse)
importFrom(rtracklayer, liftOver, import.chain) # used in convertBuild
importFrom(stringr, str_sub) # used in checkABalleles


export(readConfig, writeConfig, setConfigDefaults,
       boxplotMeanSD, plot2DwithHist, bestLegendPos,
       dbgapScanAnnotation, dbgapSnpAnnotation,
       make.allele.mappings, make.allele.annotation,
       defineFamilies, igvBatch, igvPlot,
       GenotypeReader, IntensityReader,
       plateLayoutPlot,
       findRelSet,
       hweSimulate,
       hweSimulateGenotypeMatrix,
       quadSolveMAF,
       qqPlotPng, manhattanPlotPng,
       gdsCombine, checkGdsCombine,
       gdsMerge, gdsMergeCheck,
       getSnpNames, readSnpTable,
       convertBuild, checkABalleles,
       chromToChar, chromToInt,
       gdsUpdateBuild,
       filterImputationSegments,
       getAnomSegOverlap,
       calculateLambda,
       defineDupVars
       )
