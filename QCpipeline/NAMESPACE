import(GWASTools)
import(gplots)
import(grid)
import(gridBase)
import(hexbin)
import(Biostrings)
import(tools)
import(SRAdb)
import(GWASExactHW)

export(readConfig, writeConfig, setConfigDefaults,
       boxplotMeanSD, plot2DwithHist, bestLegendPos,
       dbgapScanAnnotation, dbgapSnpAnnotation,
       make.allele.mappings, make.allele.annotation,
       defineFamilies, igvBatch, igvPlot,
       GenotypeReader,
       plateLayoutPlot,
       findRelSet,
       hweSimulate,
       hweSimulateGenotypeMatrix,
       quadSolveMAF,
       qqPlotPng, manhattanPlotPng,
       gdsMerge, gdsMergeCheck,
       getSnpNames, readSnpTable)
